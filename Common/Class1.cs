namespace Common
{
    /**
     * The Class Library is a Project which is not will be run [not executable]
     * When build it the compiler will generate the code to il [dll] file
     * there are no jitting will happen no native code
     * but the c sharp project will go through the clr and out put will be the native code
     * we use it to share the common code between projects
     */
    public class Class1
    {

    }
}
